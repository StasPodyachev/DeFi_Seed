import { createTheme } from '@mui/material/styles';

const { breakpoints, typography, spacing, palette } = createTheme();
const { pxToRem } = typography;

export { breakpoints, pxToRem, spacing, palette };
