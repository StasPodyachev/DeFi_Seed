import { spacing } from './handlers';

const MuiDialogActionsTheme = {
  styleOverrides: {
    root: {
      padding: spacing(1, 2, 2),
      justifyContent: 'center',
    },
  },
};

export default MuiDialogActionsTheme;
