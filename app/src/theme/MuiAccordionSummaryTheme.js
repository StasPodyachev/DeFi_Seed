import { spacing } from './handlers';

const MuiAccordionSummaryTheme = {
  styleOverrides: {
    root: {
      padding: spacing(0, 1),
    },
    content: {
      margin: 0,
    }
  },
};

export default MuiAccordionSummaryTheme;
